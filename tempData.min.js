export default tempData=[];export function updateTodos(index,cards){tempData.length=0;cards.forEach(card=>{tempData.push({id:card.id,name:card.name,color:card.color,todos:card.todos,completed:card.state,idChecklist:card.idChecklist})})}